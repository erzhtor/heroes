@{
    ViewBag.Title = "Heroes IMS";
}

<div id="app">
    <h1>Heroes Information System</h1>
    <ul></ul>
    <div class="navbar navbar-default navbar-fixed-top">
        <div class="container">
            <div class="navbar-header">
                <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                <router-link tag="a" to="/list-heroes" class="navbar-brand">Heroes IMS</router-link>
            </div>
            <div class="navbar-collapse collapse">
                <ul class="nav navbar-nav">
                    <router-link tag="li" to="/list-heroes" active-class="active"><a>List of Heroes</a></router-link>
                    <router-link tag="li" to="/create-hero" active-class="active"><a>Add new Hero</a></router-link>
                </ul>
            </div>
        </div>
    </div>
    <div class="row">
        <router-view></router-view>
    </div>
</div>

@Html.Partial("_CreateHeroTemplate")
@Html.Partial("_ListHeroesTemplate")

@Scripts.Render("~/VueApp/bundle.js")
